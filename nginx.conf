server {
  listen 80;
  server_name _;

  # Flutter shell at root
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }

  # Phaser game at /game/
  location /game/ {
    alias /usr/share/nginx/html/game/;
    try_files $uri $uri/ /game/index.html;
  }
}
