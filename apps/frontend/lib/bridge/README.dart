// bridge/ — Shell ↔ Phaser game bridge transport layer.
// Contains the Dart-side message types (bridge_messages.dart) and platform-specific
// transports (bridge_mobile.dart for JavaScriptChannel, bridge_web.dart for postMessage).
//
// This directory is owned by sub-track B (bridge agent). Do not add files here
// from the UI sub-track — leave TODOs referencing B01/B02/B03 tasks instead.
