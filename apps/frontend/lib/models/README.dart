// ignore_for_file: file_names
// models/ — Typed data models shared across screens (e.g. UserProfile, MatchResult).
// Models are plain Dart classes — no Flutter dependencies, no platform channels.
// Auth-derived models (userId, sessionToken) are defined here as value objects;
// their population is delegated to services/.
